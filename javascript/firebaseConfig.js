// Firebase configuration
const firebaseConfig = {
    databaseURL: "https://join-portfolio-7b325-default-rtdb.europe-west1.firebasedatabase.app/"
};

// Initialize Firebase
firebase.initializeApp(firebaseConfig);
const database = firebase.database();
